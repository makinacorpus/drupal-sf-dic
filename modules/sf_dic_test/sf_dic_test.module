<?php
/**
 * @file
 * Yes, it is a Drupal module.
 */

/**
 * Implements hook_menu().
 */
function sf_dic_test_menu() {
  $items = [];

  $items['test/form/implements'] = [
    'page callback'     => 'sf_dic_page_form',
    'page arguments'    => ['MakinaCorpus\Drupal\Sf\Container\Tests\Mockup\FormImplements', "42"],
    'access callback'   => true,
    'type'              => MENU_CALLBACK,
  ];

  $items['test/form/extends'] = [
    'page callback'     => 'sf_dic_page_form',
    'page arguments'    => ['MakinaCorpus\Drupal\Sf\Container\Tests\Mockup\FormExtends', 'foo'],
    'access callback'   => true,
    'type'              => MENU_CALLBACK,
  ];

  return $items;
}
